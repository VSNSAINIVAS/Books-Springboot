// package which contains the code which validates
package org.zeta.validate;
// Class validation
public class Validation {
    // Checking if object is null or not
    public static boolean checkNull(Object ob){
        if(ob == null) return true;
        return false;
    }
}
